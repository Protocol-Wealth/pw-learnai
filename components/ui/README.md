# UI primitives

Reserved for shared primitives (buttons, cards, layout) if a component grows large enough to need them.

For now, components in `interactive/` are deliberately self-contained — each one is a single .jsx file with no internal dependencies. If you find yourself wanting to share UI logic between components, add it here.
