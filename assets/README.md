# Assets

Images, diagrams, and SVGs referenced from module markdown files. Keep them small and check them into git directly — no LFS, no external CDN dependencies.

Naming convention: `{module-number}-{short-description}.{ext}`. Example: `01-disruption-quadrants.svg`.
